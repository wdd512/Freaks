import Link from "next/link";
import { getDb } from "@/db";
import { listFreaks } from "@/services/game/queries";
import { formatPercentPpm, formatSim, labelize } from "@/components/ui/format";

export const dynamic="force-dynamic";
export default function LeaderboardPage(){const freaks=listFreaks(getDb());const pnl=[...freaks].sort((a,b)=>b.equityCents-a.equityCents);const rating=[...freaks].sort((a,b)=>b.rating-a.rating);const table=(rows:typeof freaks,type:"PNL"|"RATING")=><div className="table-wrap"><table className="data-table"><thead><tr><th>RANK</th><th>FREAK</th><th>CAREER TIER</th>{type==="PNL"?<><th>CAREER RETURN</th><th>EQUITY</th><th>MAX DD</th></>:<><th>RATING</th><th>AVG SKILL</th></>}<th>WIN RATE</th><th>SESSIONS</th></tr></thead><tbody>{rows.map((freak,index)=><tr key={freak.tokenId}><td className={index<3?`rank-${index+1}`:""}>#{index+1}</td><td><Link href={`/freaks/${freak.tokenId}`}>#{freak.tokenId} {freak.name}</Link></td><td>{labelize(freak.careerLevel)}</td>{type==="PNL"?<><td className={freak.careerReturnPpm>=0?"positive":"negative"}>{formatPercentPpm(freak.careerReturnPpm)}</td><td>{formatSim(freak.equityCents)}</td><td>{(freak.maxDrawdownPpm/10000).toFixed(1)}%</td></>:<><td>{Math.round(freak.rating)}</td><td>{freak.averageSkill.toFixed(3)}</td></>}<td>{freak.wins+freak.losses?`${(freak.wins/(freak.wins+freak.losses)*100).toFixed(1)}%`:"—"}</td><td>{freak.settledSessions}</td></tr>)}</tbody></table></div>;return <main className="shell"><div className="section-heading"><div><div className="kicker">TWO WAYS TO COPE</div><h1 className="page-title">LEADERBOARDS</h1></div></div><section className="section"><div className="section-heading"><h2>CAREER PNL</h2><span className="muted">RISK INCLUDED</span></div>{table(pnl,"PNL")}</section><section className="section"><div className="section-heading"><h2>TRADER RATING</h2><span className="muted">RISK-INDEPENDENT DIRECTIONAL SKILL</span></div>{table(rating,"RATING")}</section></main>}
